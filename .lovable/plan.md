

# แผนเพิ่มระบบแจ้งเตือนผ่าน LINE

## ภาพรวม

เมื่อมีคนทำแบบสอบถามเสร็จ ระบบจะส่งข้อความแจ้งเตือนเข้า LINE กลุ่มโดยอัตโนมัติ โดยใช้ **LINE Notify** ซึ่งเป็นบริการฟรีที่ออกแบบมาสำหรับส่งข้อความเข้ากลุ่มได้ง่าย

---

## สิ่งที่ต้องเตรียม (ผู้ใช้ทำเอง)

1. เข้าไปที่ https://notify-bot.line.me/
2. ล็อกอินด้วย LINE Account
3. กดสร้าง Token ใหม่ เลือกกลุ่ม LINE ที่ต้องการรับแจ้งเตือน
4. คัดลอก Token มาใส่ในระบบ

---

## ขั้นตอนการทำงาน

### ขั้นที่ 1: เก็บ LINE Notify Token เป็น Secret

- เพิ่ม secret ชื่อ `LINE_NOTIFY_TOKEN` ในระบบ backend

### ขั้นที่ 2: แก้ไข Edge Function `notify-new-response`

เพิ่มการส่ง LINE Notify ต่อจากระบบเดิม (อีเมล + in-app notification) โดย:

- หลังจากบันทึก response สำเร็จ จะส่งข้อความเข้า LINE กลุ่ม **ทุกครั้ง** ที่มีคนตอบแบบสอบถาม
- ข้อความจะประกอบด้วย:
  - ชื่อแบบสอบถาม
  - ชื่อผู้ตอบ (หรือ "ไม่ระบุตัวตน")
  - เวลาที่ส่ง
  - สรุปคะแนน (ถ้ามีคะแนนต่ำจะแจ้งเตือนเพิ่ม)
- ใช้ LINE Notify API: `POST https://notify-api.line.me/api/notify` พร้อม Header `Authorization: Bearer <TOKEN>`

### ขั้นที่ 3: เพิ่มตัวเลือกเปิด/ปิด LINE Notify ในหน้า Admin Settings (ถ้าต้องการ)

- เพิ่มฟิลด์ `line_notify_enabled` ในตาราง `admin_settings`
- แสดง toggle เปิด/ปิดในหน้าตั้งค่า

---

## ตัวอย่างข้อความที่จะส่งเข้า LINE กลุ่ม

```text
📋 มีคนตอบแบบสอบถามใหม่!
━━━━━━━━━━━━━━━
📝 แบบสอบถาม: ความพึงพอใจบริการ
👤 ผู้ตอบ: สมชาย ใจดี
📧 อีเมล: somchai@email.com
🕐 เวลา: 11/02/2026 14:30
━━━━━━━━━━━━━━━
⚠️ พบ 2 ข้อที่คะแนนต่ำกว่าเกณฑ์!
```

---

## รายละเอียดทางเทคนิค

### ไฟล์ที่ต้องแก้ไข

| ไฟล์ | การเปลี่ยนแปลง |
|------|---------------|
| `supabase/functions/notify-new-response/index.ts` | เพิ่มฟังก์ชันส่ง LINE Notify หลังจากประมวลผล response |

### Secret ที่ต้องเพิ่ม

| ชื่อ | คำอธิบาย |
|------|----------|
| `LINE_NOTIFY_TOKEN` | Token จาก LINE Notify ที่เชื่อมกับกลุ่ม LINE |

### LINE Notify API

```text
POST https://notify-api.line.me/api/notify
Headers:
  Authorization: Bearer <LINE_NOTIFY_TOKEN>
  Content-Type: application/x-www-form-urlencoded
Body:
  message=<ข้อความแจ้งเตือน>
```

### Flow การทำงาน

```text
ผู้ตอบส่งแบบสอบถาม
       |
       v
SurveyPage -> supabase insert -> invoke notify-new-response
       |
       v
notify-new-response:
  1. ดึงข้อมูล response + survey title
  2. ส่งอีเมลขอบคุณผู้ตอบ (เดิม)
  3. ตรวจคะแนนต่ำ + สร้าง notification (เดิม)
  4. ส่งอีเมลแจ้ง admin (เดิม)
  5. [ใหม่] ส่ง LINE Notify เข้ากลุ่ม
```

