

# ตั้งค่า LINE Group Bot และเพิ่มระบบแจ้งเตือนเข้ากลุ่ม LINE

## ภาพรวม

นำ LINE Channel Access Token และ Group ID ที่ได้มาตั้งค่าในระบบ แล้วแก้ไข Edge Function ให้ส่งข้อความแจ้งเตือนเข้ากลุ่ม LINE อัตโนมัติทุกครั้งที่มีคนตอบแบบสอบถาม

---

## ขั้นตอนที่ 1: เพิ่ม Secrets

เพิ่ม 2 ค่าในระบบ backend:

| Secret | คำอธิบาย |
|--------|----------|
| `LINE_CHANNEL_ACCESS_TOKEN` | Token สำหรับ Messaging API |
| `LINE_GROUP_ID` | ID ของกลุ่ม LINE ที่ต้องการรับแจ้งเตือน |

---

## ขั้นตอนที่ 2: แก้ไข Edge Function `notify-new-response`

เพิ่มการส่งข้อความเข้ากลุ่ม LINE ต่อจากระบบเดิม โดย:

- ดึงชื่อแบบสอบถาม (survey title) จากตาราง `surveys`
- สร้างข้อความแจ้งเตือนภาษาไทย
- ส่งผ่าน LINE Messaging API: `POST https://api.line.me/v2/bot/message/push`
- ส่งทุกครั้งที่มีคนตอบแบบสอบถาม (ไม่ต้องรอคะแนนต่ำ)
- ถ้ามีคะแนนต่ำจะแจ้งเตือนเพิ่มในข้อความเดียวกัน

### ตัวอย่างข้อความ

```text
📋 มีคนตอบแบบสอบถามใหม่!
━━━━━━━━━━━━━━━
📝 แบบสอบถาม: ความพึงพอใจบริการ
👤 ผู้ตอบ: สมชาย ใจดี
📧 อีเมล: somchai@email.com
🕐 เวลา: 11/02/2026 14:30
━━━━━━━━━━━━━━━
⚠️ พบ 2 ข้อที่คะแนนต่ำกว่าเกณฑ์!
```

---

## รายละเอียดทางเทคนิค

### ไฟล์ที่แก้ไข

| ไฟล์ | การเปลี่ยนแปลง |
|------|---------------|
| `supabase/functions/notify-new-response/index.ts` | เพิ่มฟังก์ชันส่ง LINE Messaging API push message หลังจากประมวลผลเสร็จ |

### LINE Messaging API Push

```text
POST https://api.line.me/v2/bot/message/push
Headers:
  Authorization: Bearer <LINE_CHANNEL_ACCESS_TOKEN>
  Content-Type: application/json
Body:
  {
    "to": "<LINE_GROUP_ID>",
    "messages": [{ "type": "text", "text": "ข้อความแจ้งเตือน" }]
  }
```

### Flow การทำงาน (เพิ่มขั้นตอนที่ 5)

```text
notify-new-response:
  1. ดึงข้อมูล response + survey title (เพิ่มดึง title)
  2. ส่งอีเมลขอบคุณผู้ตอบ (เดิม)
  3. ตรวจคะแนนต่ำ + สร้าง notification (เดิม)
  4. ส่งอีเมลแจ้ง admin (เดิม)
  5. [ใหม่] ส่ง LINE push message เข้ากลุ่ม
```

